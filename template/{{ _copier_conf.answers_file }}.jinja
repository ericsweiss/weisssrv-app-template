# Answers this repository was generated from. `copier update` replays them
# against a newer template tag, so a template fix arrives as a reviewable diff.
# Edit an answer here only alongside the render it implies — the file is the
# record, not the switch.
---
{{ _copier_answers | to_nice_yaml -}}
