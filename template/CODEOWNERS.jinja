# Code ownership for {{ change_request }} review routing.
* @{{ git_namespace }}

# Deployment surface — retarget these to a stricter reviewer group if you have one.
/kubernetes/  @{{ git_namespace }}
